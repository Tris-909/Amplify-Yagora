import React from "react";
// import { Loading, Tabs, Icon } from "element-react";

const MarketPage = (props) => {
  return(
    <div>
      MarketPage { props.marketId }
    </div>
  )
}

export default MarketPage;
